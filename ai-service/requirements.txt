# AI Event Photo Finder — AI service dependencies.
# Tested against Python 3.12 on Windows. Install with:
#   py -3.12 -m venv .venv
#   .venv\Scripts\activate
#   pip install -r requirements.txt
#
# Note: insightface pulls in onnxruntime (CPU). The face model (~300MB) is
# downloaded automatically on first run and cached under ~/.insightface.

fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

numpy==1.26.4
opencv-python-headless==4.10.0.84
onnxruntime==1.20.1
insightface==0.7.3
